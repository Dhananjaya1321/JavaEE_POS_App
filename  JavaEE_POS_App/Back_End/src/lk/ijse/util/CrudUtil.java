package lk.ijse.util;

public class CrudUtil {
    public static <T> T setQuery(String query, Object... args) {
        if (query.startsWith("SELECT") || query.startsWith("select")) {
            return null;
        } else {
            return null;
        }
    }
}
